(function(F){
/*
	var focusId, isRunning = false;
	function init(focusId, eventName){
		focusId = focusId;
		$(".awesomeboard:first").delegate(".twatboard", "click", clickedFocusTweet);
		F.eventHub.subscribe(eventName, function(){
			switchFocusTweetWithFirstInSidebar($(focusId));
		})   
	}     
	function clickedFocusTweet(){
		insertNewFocusTweetWithAnimation
	}
	function generateUserCSSFromTweet(node){
		var obj = {};
		if(node.data("profile_text_color")){
			obj.color = "#" + node.data("profile_text_color");
		}
		if(node.data("profile_sidebar_border_color")){
			obj.backgroundColor = "#" + node.data("profile_sidebar_border_color") + " !important";
		}
		if(node.data("profile_background_image_url")){
			obj.backgroundImage = "url(" + node.data("profile_background_image_url") + ") !important;"
		}
		return obj;
	}
	function insertNewFocusTweetWithAnimation(target, asideTweet){
		
	}
	var api = {
		initialize: init
	};
	F.feedback = F.feedback || {};
	$.extend(F.feedback, {animation:api});*/
})(FINN || {});
//$(document).ready(function(){FINN.feedback.animation.initialize(".twatboard:first", "");});